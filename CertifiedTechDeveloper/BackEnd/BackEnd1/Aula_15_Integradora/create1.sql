CREATE TABLE IF NOT EXISTS clinica(id INT AUTO_INCREMENT PRIMARY KEY, nome VARCHAR(100), sobrenome VARCHAR(100), rg VARCHAR(100), dataCadastro VARCHAR(100));
CREATE TABLE IF NOT EXISTS endereco(id INT AUTO_INCREMENT FOREIGN KEY, rua VARCHAR(255), numero INT, cidade VARCHAR(255), bairro VARCHAR(255));