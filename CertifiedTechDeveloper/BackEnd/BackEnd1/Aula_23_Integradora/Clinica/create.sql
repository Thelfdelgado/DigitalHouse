CREATE TABLE IF NOT EXISTS usuarios(id INT AUTO_INCREMENT PRIMARY KEY, nome VARCHAR(100), email VARCHAR(100), senha VARCHAR(100), nivelAcesso INT, idDentista INT FOREIGN KEY REFERENCES dentista(id));
CREATE TABLE IF NOT EXISTS dentistas(id INT AUTO_INCREMENT PRIMARY KEY, nome VARCHAR(100), numMatricula INT, atendeConvenio BIT);